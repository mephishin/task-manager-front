# Разработчику
## Локальный запуск приложения
### Запуск
- Cкачать, установить и запустить Docker
- Выполнить команду
```
cd mockserver
docker compose -f mockserver-docker-compose.yml up -d
```

### Остановка
```
docker compose -f mockserver-docker-compose.yml down
```
